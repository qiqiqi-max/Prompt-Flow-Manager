# 更新日志

本文件记录每个发布版本的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

## [1.5.0] - 2026-09-20

1.4.0 是一轮集中修缺陷，这一版换了方向：补上"出了问题能查、能自己恢复"的底子，
把运行时姿态收紧到默认安全，并升掉已经停止接收安全更新的 Electron 28。

同时修掉三个**代码看着对、实际从来没生效过**的缺陷。这类失效不报错，读源码只会觉得有做，
所以它们各自的成因都写进了 `CLAUDE.md` 的「踩过的坑」。

### 新增

- **启动自愈**：启动时体检数据目录，处理三类残留——回收站索引里有条目但正文已丢（点恢复
  永远失败的幽灵记录，摘掉索引引用）、`.trash` 里有正文但索引里没条目（收养成条目让它
  重新可见）、`writeFileAtomic` 写到一半被杀留下的 `.tmp-<pid>-<seq>-*.part`（删掉）。
  原则是只做加法和摘引用，唯一会被删的是按定义就没有完整内容的半截临时文件；
  判定严格按写入时生成的文件名形态来，不会误删用户自己的 `.part` 文件。
- **滚动文件日志**：运行记录写入数据目录的 `logs/`，单文件 2 MB、最多 5 个，超出自动轮转。
  启动链路和自愈结果不再只活在控制台——用户报障时拿得出东西。写日志失败静默丢弃，
  不会因为记不了日志而影响功能。
- **诊断信息导出**：设置里可导出一份脱敏的运行环境信息（版本、路径、沙箱状态、日志尾部
  64 KB）。正文、文件名、提示词内容一概不收集。
- **升级提示**：启动时查询 GitHub 最新发布版本，有新版就在右下角常驻提示，可手动关掉或
  跳过该版本。只读版本号，**不自动下载也不安装**；8 秒超时、响应体上限 256 KB、
  每天最多查一次、失败静默；请求头只有固定三项，不带任何本机标识；发布页地址是**本地常量**，
  不取响应里的 URL。设置里可完全关闭，关掉之后连请求都不发。
- **配色与焦点可见性的自动化门禁**（`npm run test:contrast`）：两套主题各 34 对颜色逐对
  按 WCAG 2.1 计算相对亮度，正文 4.5:1、非文字 3:1；opacity 淡化的功能性图形要先与真实
  底色混合再算。自带反向对照——拿改动前的旧配色跑同一批断言，必须红 30 对以上。

### 变更

- **渲染进程沙箱默认开启**。原先无条件 `--no-sandbox`，理由是"某些缺运行库的环境开了会
  缺 DLL"，代价是所有人都失去一层隔离。现在默认开，只有连续两次启动都确认渲染进程起不来
  才降级，降级状态跨启动记住，7 天后自动重试一次（用户装上运行库就能恢复）。
  降级原因写进日志，可用 `PFM_SANDBOX=on|off` 临时覆盖。
- **Electron 28.3.3 → 43.7.2**（内置 Node 24 / Chromium 150）。28 已停止接收安全更新，
  而这个应用会渲染用户导入的 Markdown。版本在 `package.json` 里锁成确定版本不带 `^`，
  `npm test` 会拿 `node_modules/electron/dist/version` 和声明值逐字比对。
- **主进程瘦身**：1354 行测试专用代码（自检、压测、安全回归）从 `electron-main.js`
  搬进 `lib/selftest.js`，主进程 3819 → 2504 行。注入契约显式声明并在初始化时逐项校验，
  漏传当场报出名字，而不是跑到几百行之后才 `undefined is not a function`。
- 主进程原生对话框文案走 i18n，不再硬编码中文。
- 接入 eslint（只查缺陷，不管风格），并加守卫防止它被静默关掉。

### 修复 — 检查还在，但已经失效

- **`console-message` 签名变更静默杀掉整道检查**：Electron 36 把 `(e, level, message)`
  （level 是整数）改成单个 `details` 对象（level 是字符串）。这个监听器是"渲染进程报错
  就让自检失败"的**唯一**入口，签名变了之后 `level >= 2` 恒为 false，页面里报什么错都
  收集不到，自检永远全绿。现在两套签名都认，并有断言卡住两条分支都在。
- **升级了 Electron 但跑的还是旧二进制，而且全绿**：`npm i -D electron@43` 只换了包，
  二进制解压由 electron 自己的 `install.js` 负责，它开头的 `isInstalled()` 看到旧 `dist/`
  还在就直接 return。于是声明 43、实际跑 38，所有测试照常通过——升级等于没做。
  28→38→43 这轮真的这么绿过一次。现在 `npm test` 逐字比对二进制版本。
- **树行焦点环从来没渲染过一次**：`.tree-row.focused`（特异性 0,2,0）想画焦点环，
  却被紧随其后的 `.tree-row[tabindex="-1"]:focus { outline: none }`（0,3,0）盖掉——
  后者既更特殊又写在后面。键盘在树里上下移动完全看不出焦点在哪，而加 `.focused` 类的
  JS 一直在跑，读代码只会觉得有做。
- **方向键会打开折叠目录里看不见的文件**：导航筛可见行用 `:not([style*="display: none"])`，
  只认得高级筛选写的**行内**样式；而目录折叠是给 `.tree-children` 加 `hidden` **类**。
  两套隐藏机制只认出一套，折叠目录里的文件一直留在导航序列里，按一下方向键就
  `focus()` + `click()` 打开一个屏幕上不存在的行。现在按渲染结果判可见性。
  连带发现原有那条「方向键能切回上一个文件」的断言**正是靠这个 bug 才通过的**
  （它的起点行就在折叠目录里），修好过滤后它立刻变红。

### 修复 — 可访问性

- **31 对配色不达 WCAG 最低对比度**。其中搜索命中高亮 `mark` 在亮色主题下只有 2.15:1——
  用户正在搜的那段文字是屏幕上最难读的文字；`--border` 对白底 1.29:1，却在给所有输入框
  和按钮画边界。根因是一个颜色值同时承担填充色和文字色两种角色，调一个必然拖累另一个，
  所以拆成 `*-solid`（白字压在上面的填充）和 `*-text`（压在背景上的文字）两套。
  `--accent` 是唯一两套主题取同值的颜色变量，暗色 7.76:1 通过、亮色 2.15:1 惨败——
  那是漏了一份覆盖而不是设计选择。
- **五类按钮和筛选面板的控件此前没有任何焦点样式**，只靠 Chromium 默认环，而默认环颜色
  不跟主题走：没声明 `color-scheme` 时暗色主题拿到的是 `#101010` 的环，压在 `#1e1e1e`
  上 1.14:1，等于没有。现在统一走 `:focus-visible` + `--focus-ring`（鼠标点击不亮环、
  键盘才亮），并声明 `color-scheme` 让 UA 控件跟随主题。
- 代码块复制按钮平时 `opacity: 0`，键盘 Tab 进去等于"焦点停在一个看不见的按钮上"，
  现在聚焦时现身。
- 尊重 `prefers-reduced-motion`：系统开了减少动态效果就不做过渡，流程图节点也不再位移。

### 修复 — 文档与工具链

- **`CLAUDE.md` 曾把整篇文档插进自己第 7 条的正中间**，那一条的句子被截断成半句，
  肉眼翻很难发现（文档很长，前半段完全正常）。现在有断言检查文档首个 H1 是否重复出现。
- **文档漂移守卫**：文档里引用的 npm 脚本、`PFM_*` 环境开关、带斜杠的文件路径全部回查
  代码，两个方向都查——文档写的东西必须存在，代码里的每个测试脚本和开关也必须被文档记录，
  否则"加了新测试但没人知道"这种漏法下文档依然全绿。
- 自检脚本从主进程的模板字符串里抽成真实 `.js` 文件（`src/selftest/`），
  否则静态检查和 lint 都看不见它们。
- README 跟上实际状态：Electron 徽章、15 个测试脚本、`lib/` 六个模块，
  并明确写出应用唯一的出网行为和沙箱姿态。

## [1.4.0] - 2026-09-15

这一版集中修复了代码审查发现的 22 处缺陷。它们的共同点是**不报错**：出问题时界面显示成功，
只有重启或下次打开文件才发现内容不对。建议所有用户升级。

每一项修复都配了会真正变红的回归断言，并逐个做过**分离反向对照**（只撤掉这一个修复、
确认对应断言变红、其余保持绿），以排除空断言。

### 修复 — 静默丢数据

- **关窗丢改动**：侧边栏宽度、展开的目录、标签页、最近打开、锁定列表这五项都走防抖写入，
  改完立刻关窗那一次会丢失。现在关窗前先把待写入的内容落盘再真正关闭，并带 3 秒超时兜底，
  避免页面卡死时窗口关不掉。
- **切文件时静默强制保存**：切换文件或标签页原先直接强制落盘，而保存会自动改写 frontmatter
  字段、写版本快照、递增版本号，全都不可逆。现在改为「保存 / 不保存 / 取消」三选一，
  按 Esc 或关闭对话框等于取消。
- **配置写入失败没有提示**：主题、语言、锁定列表这些改动看起来生效了，重启后全部回滚。
  现在写入失败会明确提示。
- **新建与导入可能静默覆盖**：判重和写入原先是两步，并发时几路都能通过判重并先后写入，
  后一次把前一次正文整个盖掉，而每路都返回成功——用户看到「导入成功 5 条」，磁盘上只剩一条。
  现在用内核级的独占创建把判重和写入合成一个原子操作，导入撞名后重新挑名字，
  保持「自动改名不覆盖」的既有行为。
- **保存正文前读旧内容失败仍继续写**：现在放弃保存并报错，不再用不完整的内容覆盖用户正文。

### 修复 — 并发竞态

一类相同的形状：读取 → 在内存里计算 → 写回，三步没有串行化，并发时各自基于旧快照计算，
后写覆盖前写。界面上有五处防抖写入，一次常规操作就能插进来。

- **连续保存丢版本号与快照**：实测连发 5 次保存，版本号只从 1 涨到 2，5 条快照只落 3 条。
  按住 `Ctrl+S` 就能触发。现在按文件串行。
- **锁定列表、项目类型增删**：并发新增 5 个丢 4 个、并发删除 4 个只删 1 个。现在进同一个队列。
- **重命名的半完成中间态**：会留下文件已改名、而锁定列表和标签页还指着旧名的状态。
- **删除时读到过期的锁状态**：「锁定后立刻删除」可能读到旧值，把锁绕过去。
- **同毫秒快照互相覆盖**：快照名精度只到毫秒，同毫秒的两次保存会写到同一个名字，
  旧快照被覆盖，Windows 上还可能抛出裸的系统错误码。
- **回收站的写锁缺口**：删不掉或索引写不进时不再静默丢文件，改为明确上报。

### 安全

- **跨盘符路径逃逸**：路径包含性检查在 Windows 跨盘符时被整体绕过（跨盘符时返回的是绝对路径
  而不是一串 `..`）。工作流文件里的提示词引用会渲染成可点击节点，用户点一下就能读到内容库
  以外的文件，**不需要额外的 XSS**。
- **窗口内导航放行过宽**：预览区渲染出的相对链接和 UNC 路径链接会在应用窗口内直接打开，
  而 preload 会向新页面重新注入完整的 API、页面自身的 CSP 又只对原文档生效——
  一次点击就能读走整个内容库并外发。现在只放行界面自身那一个地址，其余交给系统浏览器。
- **回收站索引被写坏能删到库外**：清空回收站和还原原先直接拿索引里的字段拼路径。
  现在只接受单段文件名并锁定在回收站目录内。
- **还原绕过写入白名单**：能把回收站里的文件还原成配置文件或版本快照，与删除入口的标准不一致。
- **DOM clobbering 吞掉用户编辑**：预览内容里的 `id` / `name` 属性会在全局覆盖脚本变量，
  导致后续保存写到错误的地方。现在这两个属性会被过滤。
- **两处未转义的 HTML 插入**：版本时间戳与回收站时间。

### 修复 — 资源与体验

- **ZIP 导入的文件描述符泄漏**：导入失败或提前返回时描述符一直挂着，连续导入几十个包会耗尽。
- **ZIP 导入无总量上限**：原先只校验单个条目大小，一个高压缩比的小包解出来能占满磁盘。
  现在限制总量 64MB、条目数 5000。
- **正文缓存无界增长**：只读浏览过的文件会永久留在内存里，每条还存了正文加一份小写副本。
  现在按 LRU 封顶。
- **Markdown 渲染成本上限**：高分隔符密度的输入会让渲染退化成二次复杂度（实测单行 4 万个
  分隔符要 5.4 秒），且会随标签页恢复跨重启复发，表现为「打开就卡死」。现在有成本上限，
  且标签页恢复失败不会形成循环。
- **原始错误文本弹给用户**：两处直接显示主进程的错误文本，现在走本地化。

### 新增

- CI（`.github/workflows/ci.yml`，windows-latest）：7 个测试套件各占一个独立 step，
  外加一步 vendor 副本与依赖版本的一致性检查。
- `tests/debounce.test.js`：从渲染进程源码**原文截取**防抖实现来测，不抄副本。
  内置「旧版实现必须失败」的反向夹具。
- `tests/close-flush.test.js`：拉起两个 Electron，比对正常关窗与直接销毁窗口的结果，
  两组结果必须不同才算测到了落盘握手本身。
- `tests/render-guard.test.js`：渲染成本上限，自带反向对照。
- `tests/search-bench.js` 与 `npm run bench`：搜索压测。
- 未保存对话框的三条分支（保存 / 不保存 / 取消）都有落到磁盘的自动化断言。

### 变更

- `dompurify`、`marked`、`diff-match-patch` 从 `^` 范围改为精确版本。仓库里提交了
  `src/vendor/` 副本且渲染进程实际加载的是那份副本，用范围版本会导致两者不一致且没有提示。

[未发布]: https://github.com/qiqiqi-max/Prompt-Flow-Manager/compare/v1.5.0...HEAD
[1.5.0]: https://github.com/qiqiqi-max/Prompt-Flow-Manager/compare/v1.4.0...v1.5.0
[1.4.0]: https://github.com/qiqiqi-max/Prompt-Flow-Manager/releases/tag/v1.4.0
